#include <stdio.h>

int main()
{
	int a;//4바이트=int에 데이터 a를 보관
	a = 10;//변수 a에 10을 넣어라

	printf("a의 값은 %d\n", a);//%d=>변수 a의 값에 10진수로 출력해라
	return 0;
}

//이와 같이 C 언어에서, 바뀔 수 있는 어떤 값을 보관하는 곳을 변수 라고 합니다.
//영어로는 Variable 이라 하는데, 말 그대로 바뀔 수 있는 것들 이라는 뜻입니다.